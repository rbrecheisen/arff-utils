============
Installation
============

At the command line::

    $ easy_install arff_utils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv arff_utils
    $ pip install arff_utils
